spring:
  application:
    name: scheduler-app

  datasource:
    url: jdbc:mysql://localhost:3316/scheduler
    username: root
    password: scheduler
    hikari:
      maximum-pool-size: 10

springdoc:
  api-docs:
    enabled: true
    path: /api-docs-json
  swagger-ui:
    path: /swagger-ui
    url: /api-docs-json
